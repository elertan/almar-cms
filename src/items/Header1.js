import React from 'react';

const Header1 = (data) => {
    return (
        <div style={{ height: 200, background: 'blue' }}>
            {data.title}
        </div>
    );
};

export default Header1;
